{
  "name": "memily",
  "version": "1.1.2-beta",
  "description": "Javascript function memoization library.",
  "main": "dist/memily.js",
  "repository": "git@github.com:ryami333/memily.git",
  "author": "Mitch Ryan <mitch@mitch-ryan.com>",
  "license": "UNLICENSED",
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.37",
    "@babel/core": "^7.0.0-beta.37",
    "@babel/preset-es2015": "^7.0.0-beta.37",
    "@babel/preset-flow": "^7.0.0-beta.37",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^22.0.6",
    "consecutive": "^5.0.4",
    "flow-bin": "^0.63.1",
    "jest": "^22.0.6"
  },
  "scripts": {
    "test": "jest",
    "build:umd": "babel src/memily.js --out-dir dist",
    "build:flow": "yarn --silent flow gen-flow-files src/memily.js > dist/memily.flow.js",
    "prepublishOnly": "yarn flow && yarn test && yarn build:umd && yarn build:flow && git stage dist/*"
  }
}
